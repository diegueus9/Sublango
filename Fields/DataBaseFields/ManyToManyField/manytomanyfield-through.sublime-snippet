<snippet>
	<content><![CDATA[
${1:name} = models.ManyToManyField('${2:model}', through='${3:through}', related_name='${1:name}s', blank=True, null=True, verbose_name=_('${1:name}'))

class ${3:through}(models.Model):
    model_from = models.ForeignKey('model_from')
    ${2:model} = models.FoerignKey('${2:model}')

]]></content>
	<tabTrigger>djmanythrough</tabTrigger>
	<scope>source.python</scope>
</snippet>
